import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: "#FFF8E1", position: "relative" },
  dropdown: {
    borderWidth: 1,
    borderColor: "gray",
    borderRadius: 8,
    padding: 15,
  },
});

export default styles;
